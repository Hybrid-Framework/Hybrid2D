<Project Sdk="Microsoft.NET.Sdk">

    <PropertyGroup>
        <OutputType>Exe</OutputType>
        <TargetFramework>net10.0</TargetFramework>
        <AllowUnsafeBlocks>true</AllowUnsafeBlocks>
        <ImplicitUsings>enable</ImplicitUsings>
        <Nullable>enable</Nullable>
    </PropertyGroup>

    <ItemGroup>
        <ProjectReference Include="..\..\..\App\App.csproj" />
    </ItemGroup>

    <PropertyGroup>
        <Architecture Condition="'$(RuntimeIdentifier)' != ''">$(RuntimeIdentifier)</Architecture>
        <Architecture Condition="'$(RuntimeIdentifier)' == ''">linux-$([System.Runtime.InteropServices.RuntimeInformation]::OSArchitecture.ToString().ToLower())</Architecture>
    </PropertyGroup>

    <ItemGroup>
        <Content Remove="$(SolutionDir)\Natives\Desktop\Linux\**\*"/>
        <Content Include="$(SolutionDir)\Natives\Desktop\Linux\$(Architecture)\**\*">
            <CopyToPublishDirectory>PreserveNewest</CopyToPublishDirectory>
            <CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory>
            <TargetPath>%(Filename)%(Extension)</TargetPath>
            <Visible>false</Visible>
        </Content>
    </ItemGroup>

</Project>
